<h1>Songs</h1>

<% @song.each do |song| %>
    <p><%= link_to song.name_song, songs_path(song.id) %></p>
<% end %>

<%= link_to "[New song]", new_artist_path %>

<h1>songs</h1>

<% @songs.each do |song| %>
  <p><%= link_to song.name, song_path(song.id) %></p>
<% end %>

<hr>

<%= link_to "[New Artist]", new_artist_path %>
